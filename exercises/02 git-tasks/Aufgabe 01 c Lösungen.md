**Lösungen**:

```bash
# Ignorierte Dateien
git status --ignored

# Änderungen der letzten Woche
git log --since="1 week ago" --oneline

# Historie der composer.json
git log -p composer.json
git blame composer.json
```
